{% extends 'base.html.twig' %}

{% block title %}Consola{% endblock %}

{% block body %}
    <h1>Consola</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consola.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ consola.Nombre }}</td>
            </tr>
            <tr>
                <th>Compañia</th>
                <td>{{ consola.Compania }}</td>
            </tr>
            <tr>
                <th>Fecha de salida</th>
                <td>{{ consola.FechaSalida ? consola.FechaSalida|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consola_index') }}"
       class="btn btn-secondary btn-md active" role="button" aria-pressed="true">Volver a la lista</a>
    <br/>
    <a href="{{ path('consola_edit', {'id': consola.id}) }}"
       class="btn btn-primary btn-md active" role="button" aria-pressed="true">Editar</a>

    {{ include('consola/_delete_form.html.twig') }}
{% endblock %}
